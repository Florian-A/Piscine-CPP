{
    "version": "1.0.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Array",
            "program": "${workspaceFolder}/Array",
            "args": [
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "env": {
                "PATH": "${env:PATH}"
            },
        }
    ]
}